<?xml version="1.0" encoding="UTF-8" ?>
<mvvm:BaseView xmlns="http://xamarin.com/schemas/2014/forms"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:mvvm="clr-namespace:Demo.Movie.Views.MVVM"
               xmlns:vm="clr-namespace:Demo.Movie.Core.ViewModels;assembly=Demo.Movie.Core"
               xmlns:controls="clr-namespace:Demo.Movie.Controls"
               x:Class="Demo.Movie.Views.LandingPage"
               x:TypeArguments="vm:LandingPageViewModel">
    <ContentPage.Content>
        <StackLayout>
            <ScrollView>

                <controls:GenreCarouselGridView x:Name="GenreGrid"
                                                ItemsSource="{Binding FilmsByGenre}"
                                                WidthDefinition="*"
                                                HeightDefinition="*"
                                                RowSpacing="12">
                    <controls:GenreCarouselGridView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout>
                                <Label Text="{Binding name}"
                                       HorizontalOptions="Start"/>

                                <CarouselView ItemsSource="{Binding associated_films}">
                                    <CarouselView.ItemTemplate>
                                        <DataTemplate>
                                            <Frame BorderColor="{StaticResource Key=LightPurple}"
                                                   HeightRequest="80"
                                                   WidthRequest="60"
                                                   CornerRadius="10"
                                                   Padding="0">
                                                <ImageButton Source="{Binding poster_path}"
                                                             Margin="0"
                                                             HeightRequest="80"
                                                             WidthRequest="60"/>
                                            </Frame>
                                        </DataTemplate>
                                    </CarouselView.ItemTemplate>
                                </CarouselView>

                            </StackLayout>
                        </DataTemplate>
                    </controls:GenreCarouselGridView.ItemTemplate>
                </controls:GenreCarouselGridView>

            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</mvvm:BaseView>
